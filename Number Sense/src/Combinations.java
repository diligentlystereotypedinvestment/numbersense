/*
 * public class Combinations { int choose (int left, int right) { Factorial fac
 * = new Factorial (); int num = fac.fac (left); int denom = fac.fac (right) *
 * fac.fac (left - right); return num / denom; } }
 */
